server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server

  cloud:
    config:
      server:
        git:
          # git file with file protocol
          uri: file:///Users/DongKuen/Desktop/Github/spring_cloud_config